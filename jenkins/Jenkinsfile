pipeline {
    agent any
    tools{
        maven 'apache-maven-3.9.3'
    }
    stages{
        stage("Checkout the project") {
           steps{
               git branch: 'DEV', credentialsId: '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', url: 'https://github.com/ashokvb/Simplilearn'
           } 
        }
        stage("Build the Maven package"){
            steps {
                sh 'mvn clean package'
            }
        }
    }
}
